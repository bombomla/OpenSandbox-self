# Local configuration (contains credentials) - copy from audit.toml.example
audit.toml
config

# Frontend build inputs/outputs
frontend/node_modules/

# Python caches (config.py/k8s.py live in utils/, bytecode lands next to them)
__pycache__/
.pytest_cache/
